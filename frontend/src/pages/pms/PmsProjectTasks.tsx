import { PmsProjectTaskListView } from "@/components/pms/PmsProjectTaskListView";

/** Single project tasks page — ClickUp-style list grouped by status. */
export default function PmsProjectTasks() {
  return <PmsProjectTaskListView />;
}
